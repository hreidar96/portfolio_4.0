# Copy to .env.local and fill in. Also set these in Vercel (Production + Preview).

# Sanity project (not secret)
NEXT_PUBLIC_SANITY_PROJECT_ID="7mzw821b"
NEXT_PUBLIC_SANITY_DATASET="production"

# Sanity token with Viewer role, for Draft Mode / Visual Editing (secret)
SANITY_API_READ_TOKEN=""

# Resend API key for the contact form (secret)
RESEND_API_KEY=""
